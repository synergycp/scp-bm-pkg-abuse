{
  "report": {
    "threshold": {
      "LABEL": "Maximum Age of Reports",
      "PLACEHOLDER": "During import, reports older than this many days will be ignored."
    }
  },
  "auth": {
    "host": {
      "LABEL": "IMAP Host"
    },
    "user": {
      "LABEL": "IMAP User"
    },
    "pass": {
      "LABEL": "IMAP Password"
    },
    "archive_folder": {
      "LABEL": "Archive Folder",
      "PLACEHOLDER": "IMAP folder to move processed emails to. Leave empty to auto-detect: Gmail and Google Workspace use [Gmail]/All Mail, other providers use Archived."
    }
  },
  "email": {
    "enabled": {
      "LABEL": "Send abuse emails",
      "PLACEHOLDER": "When this is enabled, SynergyCP will send an email to clients every time an abuse report is assigned to one of their servers (including through auto-detection via IP). The email that is sent can be edited from System > Emails > abuse_report.tpl."
    }
  },
  "sync_frequency": {
    "LABEL": "Sync Frequency (Minutes)",
    "PLACEHOLDER": "How often to check for new abuse emails, in minutes. Default: 5."
  },
  "auto_suspension": {
    "LABEL": "Auto Suspension",
    "PLACEHOLDER": "If a report is not responded to by the client within this many days, the server will be suspended (unless it is a VIP client). Leave empty to disable auto suspension."
  }
}
